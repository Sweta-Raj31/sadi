import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import api from './api.js'; import './style.css';
function App(){const [products,setProducts]=useState([]),[email,setEmail]=useState(''),[password,setPassword]=useState(''); const load=()=>api.get('/products').then(r=>setProducts(r.data.items)); useEffect(()=>{load()},[]); const login=async e=>{e.preventDefault();const r=await api.post('/auth/login',{email,password});localStorage.setItem('token',r.data.token);alert('Logged in')}; const add=async id=>{if(!localStorage.getItem('token'))return alert('Login first');await api.post('/cart/items',{productId:id,quantity:1});alert('Added to cart')}; return <main><h1>OrderFlow</h1><p>MERN e-commerce demo</p><form onSubmit={login}><input placeholder="email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Login</button></form><section>{products.map(p=><article key={p._id}><h2>{p.name}</h2><p>{p.description}</p><b>₹{p.price}</b><p>Stock: {p.stock}</p><button onClick={()=>add(p._id)}>Add to cart</button></article>)}</section></main>}; createRoot(document.getElementById('root')).render(<App/>);